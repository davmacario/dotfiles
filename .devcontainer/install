#!/usr/bin/env bash

if [ -n "$DOTFILES_BRANCH" ]; then
    echo "Using branch $DOTFILES_BRANCH"
fi

git switch "$DOTFILES_BRANCH" --track "origin/$DOTFILES_BRANCH"

sudo "$(dirname "$0")/../setup.sh"
